import React from 'react'

const FunctionProps = (props) => {
  return (
    <div>
        <h1> function passs help with Props</h1>
        <button onClick={props.data}>Click</button>
    </div>
  )
}

export default FunctionProps